define(
    function () {

        var vm = {
            activate: activate
        };
        return vm;

        function activate() {
            // This isn't requesting the view file like it should... not sure why.
        }
    });